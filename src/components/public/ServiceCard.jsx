import { Link } from "react-router-dom";
import { ArrowRight, Clock3 } from "lucide-react";
export function ServiceCard({ service }) { return <article className="group overflow-hidden bg-card"><div className="aspect-[4/3] overflow-hidden"><img src={service.image} alt="" className="size-full object-cover transition duration-700 group-hover:scale-105" /></div><div className="p-6 md:p-7"><div className="flex items-start justify-between gap-4"><h3 className="font-display text-3xl leading-none">{service.name}</h3><span className="shrink-0 text-sm font-semibold text-accent">{service.spaPrice}</span></div><p className="mt-4 text-sm leading-6 text-muted-foreground">{service.description}</p>{service.duration && <p className="mt-4 flex items-center gap-2 text-xs uppercase tracking-wider text-muted-foreground"><Clock3 className="size-3.5" />{service.duration}</p>}<div className="mt-6 flex flex-wrap gap-4 text-xs font-semibold uppercase tracking-[.14em]"><Link to={`/services/${service.slug}`} className="inline-flex items-center gap-2 hover:text-accent">View Details <ArrowRight className="size-3.5" /></Link><Link to={`/book?service=${service.slug}`} className="border-b border-foreground pb-1 hover:border-accent hover:text-accent">Book This Session</Link></div></div></article>; }
